// ========================================================================== //

// Refresh the page upon resize.
window.onresize = function() { location.reload(); }

// Implement so that click on original canvas takes me back to home page.

// ========================================================================== //
